//
// Created by Calun on 2020/11/3.
//

#ifndef WEEK3_TOOLS_H
#define WEEK3_TOOLS_H

#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define MIN(x, y) ((x) < (y) ? (x) : (y))

void random_float(float *a, int n);
void cp_float_array(const float *a, float *b, int a_length);

#endif //WEEK3_TOOLS_H
